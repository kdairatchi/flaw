# Real work, no stubs
def add(a : Int32, b : Int32) : Int32
  a + b
end

# TODO comments about design considerations, not stubs
# TODO: consider caching this result across requests
def expensive
  compute_something
end
